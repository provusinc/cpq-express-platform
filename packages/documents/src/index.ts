/**
 * @workspace/documents — `@react-pdf/renderer` Quote Document components and a
 * render function, shared by the browser preview and server rendering.
 *
 * Placeholder: lands in #21.
 */
export {}
