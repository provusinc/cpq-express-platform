/**
 * Pricing engine (ADR-0002): line totals, Subtotal, Quote Discount, Total,
 * cost and Margin. Runs in the browser (live editing) and on the server
 * (authoritative). Implemented in a later ticket.
 */
export {}
